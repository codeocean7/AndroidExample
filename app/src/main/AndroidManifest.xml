<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zealsoft.androidexample">


    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidExample">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.AndroidExample.NoActionBar">

        </activity>

        <activity android:name=".MyBottomNavActivity"
            android:theme="@style/Theme.AndroidExample">

        </activity>

        <activity android:name=".TabActivity"
            android:theme="@style/Theme.AndroidExample">


        </activity>

        <activity android:name=".EmployeeListActivity"
            android:theme="@style/Theme.AndroidExample">

        </activity>

        <activity android:name=".MenuActivity"
            android:theme="@style/Theme.AndroidExample">

        </activity>

        <activity android:name=".DialogActivity"
            android:theme="@style/Theme.AndroidExample">

        </activity>

        <activity android:name=".FormActivity"
            android:theme="@style/Theme.AndroidExample"
            android:label="Form">

        </activity>
        <activity android:name=".UpdateEmpActivity"
            android:theme="@style/Theme.AndroidExample"
            android:label="Update ">

        </activity>
        <activity android:name=".SelectUploadImageActivity"
            android:theme="@style/Theme.AndroidExample">

        </activity>

        <activity android:name=".AuthWork.RegistrationActivity"
            android:theme="@style/Theme.AndroidExample">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".AuthWork.LoginActivity"
            android:theme="@style/Theme.AndroidExample">

        </activity>
    </application>

</manifest>